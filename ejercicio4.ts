type direcciones = "arriba"|"abajo"|"izquierda"|"derecha";

const cualDireccion = (direccional : direcciones) =>{
    return direccional
}

let arriba:direcciones = "arriba";
let abajo:direcciones = "abajo";
let derecha:direcciones = "derecha";
let izquierda:direcciones = "izquierda";

console.log(cualDireccion(arriba));
console.log(cualDireccion(abajo));
console.log(cualDireccion(derecha));
console.log(cualDireccion(izquierda));
